# Whether the queue is built, and where it lives

`wayfinder:grilling` · HITL · blocked by: *What you actually do with this app, ranked*

## Question

Type a job now, let it start when an agent frees up. Shaf named this as the part he
liked. **It does not exist** — no route, no module, no storage, no reference
anywhere in the source, checked 2026-09-02. An earlier artifact called it "mocked";
that was wrong.

So this is a build from zero, and the questions are: is it wanted enough to build,
what does a queued job hold (folder, the text, a cap on how many run at once), does
it appear as a lane on the map or a separate page, and does the queue start agents
by itself or wait for a press?

Note the timing objection: a queue pays off when agents finish and a slot opens.
Nine of twelve are currently stopped on a question, so a queue would have had
nothing to hand work to today.

**Why it blocks:** the *Next up* column in *What the board groups by* is empty
without it.
